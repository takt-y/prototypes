import type { SummarySettings } from '../../types/job'

export const summary: SummarySettings = {
  published: true,
  includeNotes: false,
  includeStates: true,
  includeCompanies: true,
  filter: 'all',
  linkToken: 'a1b2c3d4',
  revoked: false,
}
